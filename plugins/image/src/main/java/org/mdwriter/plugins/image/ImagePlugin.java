package org.mdwriter.plugins.image;

import org.pf4j.Plugin;
import org.pf4j.PluginWrapper;

public class ImagePlugin extends Plugin {

    public ImagePlugin(PluginWrapper wrapper) {
        super(wrapper);
    }
}
